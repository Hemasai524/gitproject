name: sample1 trigger

on:
  pull_request:
    branches:
      - main
    paths:
      -'project1/sample1/**'
      -'.github/workflows/**'

jobs:
  sample1:
    runs-on:ubuntu-latest
    steps:
     -uses:actions/checkout@v2
     -run:echo "sample1 triggered"


